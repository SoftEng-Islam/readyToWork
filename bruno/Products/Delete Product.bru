meta {
  name: Delete Product
  type: http
  seq: 6
  tags: [
    planned
    contract
    products
  ]
}

delete {
  url: {{apiUrl}}/products/{{productId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
