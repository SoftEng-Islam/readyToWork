meta {
  name: Session
  type: http
  seq: 3
  tags: [
    current
    auth
  ]
}

get {
  url: {{apiUrl}}/auth/session
  body: none
  auth: none
}

tests {
  test('session returns 200', function () {
    expect(res.getStatus()).to.equal(200);
  });

  test('session returns scaffold message', function () {
    const body = res.getBody();

    expect(body).to.have.property('data');
    expect(body).to.have.property('message');
    expect(body.data).to.equal(null);
  });
}
